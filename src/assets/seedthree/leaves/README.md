# Game-owned SeedThree foliage cards

`raspberry_spray_albedo.png` is a transparent botanical texture generated for
the procedural raspberry shrub. It is stored with the game because the current
SeedThree submodule does not publish the leaf spray referenced by the shrub
integration.
